source "https://rubygems.org" #where to get gems
gem "sinatra"#tells it what gem to get
ruby "2.3.0"#current installed ruby version (ruby -v)
#then go to command line and bundle install
#once teh Gemfile.lock file is created, we can commit it with git
#Then heroku create
#then git push heroku master
